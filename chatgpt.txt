你這裡提供的accumulatedHeight是有問題題的,在你的程式碼裡,我在pageIndex=0時accumulatedHeight=0,pageIndex=1時accumulatedHeight=1883,accumulatedHeight=3時,accumulatedHeight=7532,但它實際上應該是要5649而非7532
所以按照你的程式, 我在第一頁、第二頁都能正常srollTop到我要的位置,但我到第四頁時,就不正常了,它會直接滑到底,所以再確認一下你給的程式是不是有問題

  function skipToSignPosition(buttonIndex: number = 0, type: string) {
    console.log(`buttonIndex => `, buttonIndex)
    const el = scrollContainerRef.value?.$el || scrollContainerRef.value;
    if (!(el instanceof HTMLElement)) return;

    const target = signatureButton.value[buttonIndex];
    const [x, y, width, height] = target.xy.split(',').map(Number);
    const { pageIndex, pageHeight, documentHeight } = target;

    let targetTop = 0;

    if (type === 'button') {
      // ✅ 1. 累加前面頁面的 pageHeight
      const accumulatedHeight = signatureButton.value
        .filter(btn => btn.pageIndex < pageIndex)
        .reduce((sum, btn) => sum + btn.pageHeight, 0);

      // ✅ 2. 計算該頁內部 y 相對高度
      const yOffset = (pageHeight / documentHeight) * y;

      // ✅ 3. 加總後減去 navbar
      targetTop = accumulatedHeight + yOffset - navbarHeight.value;

      console.log(`y => `, y)
      console.log('📌 scrollTo 詳細資訊：');
      console.log('accumulatedHeight:', accumulatedHeight);
      console.log('yOffset:', yOffset);
      console.log('targetTop:', targetTop);
    }

    el.scrollTo({
      top: targetTop,
      behavior: 'auto',
    });
  }