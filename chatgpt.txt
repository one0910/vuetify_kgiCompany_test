我再問另個問題
下面這是我src\service\documentSignature.ts檔案,你也說我這裡寫的不錯
export const getSignatureDoc = async (): Promise<any> => {
  try {
    const response = await fetch('http://localhost:5173/mocks/InsureanceMoc.json')
    const data = await response.json()
    const newData = data.map((item: any) => ({
      ...item,
      pageHeight: 0
    }))
    return newData
  } catch (error) {
    console.log(`error => `, error)
  }
}

那我是不是可以將    const newData = data.map((item: any) => ({
      ...item,
      pageHeight: 0
    })) 這個的邏輯搬到store去寫 ,比如它就會變成  const insureanceData = ref(
    InsureanceMoc.map(doc => ({
      ...doc,
      pageHeight: 0
    }))
  ); 這樣, 你認為從後端取來的資料,若是要特別重新再整理,是寫在service好, 還是在store裡寫好?