import { defineStore } from 'pinia'
import axios from 'axios'
const url = `${import.meta.env.VITE_API_URL}`
export const useCartStore = defineStore('test', {
  state: () => ({

  }),
  actions: {

  }
})
